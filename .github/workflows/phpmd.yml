name: phpmd

on:
  pull_request:
  push:
    branches: [main]

jobs:
  phpmd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - run: composer install --no-interaction --prefer-dist
      - run: composer bin phpmd install --no-interaction --prefer-dist
      - run: composer phpmd
